package property;

public interface Cardable { // Throws an unknown error (i cant see it) but its the same as in the notes.
    public Actionable getCardAction(); // get chance/community chest card action
}
